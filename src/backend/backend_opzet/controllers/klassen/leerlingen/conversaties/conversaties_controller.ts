import { Request, Response } from "express";

export async function leerling_conversaties(req: Request, res: Response) {
  res.status(501);
}

export async function vraag(req: Request, res: Response) {
  res.status(501);
}

export async function vraag_aanmaken(req: Request, res: Response) {
  res.status(501);
}

export async function vraag_verwijderen(req: Request, res: Response) {
  res.status(501);
}

export async function beantwoord_vraag(req: Request, res: Response) {
  res.status(501);
}
