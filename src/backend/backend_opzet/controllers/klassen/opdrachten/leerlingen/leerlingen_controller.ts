import {Request, Response} from "express";

export function opdracht_leerlingen(req: Request, res: Response) {
    res.status(501);
}

export function opdracht_voeg_leerling_toe(req: Request, res: Response) {
    res.status(501);
}

export function opdracht_verwijder_leerling(req: Request, res: Response) {
    res.status(501)
}