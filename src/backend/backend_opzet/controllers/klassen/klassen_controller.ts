import {Request, Response} from "express";

export function maak_klas(req: Request, res: Response) {
    res.status(501);
}

export function klas(req: Request, res: Response) {
    res.status(501);
}

export function verwijder_klas(req: Request, res: Response) {
    res.status(501);
}