import {Request, Response} from "express";

export function aanmelden_leerling(req: Request, res: Response) {
    res.status(501).send();
}

export function aanmelden_leerkracht(req: Request, res: Response) {
    res.status(501);
}