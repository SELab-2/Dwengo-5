import {Request, Response} from "express";

export async function aanmelden_leerling(req: Request, res: Response) {
    res.status(501).send();
}

export async function aanmelden_leerkracht(req: Request, res: Response) {
    res.status(501);
}