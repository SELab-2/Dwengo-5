model Student {
  id               Int                     @id
  user             User                    @relation(fields: [id], references: [id])
  groups           StudentGroup[]
  learning_objects StudentLearningObject[]
}
