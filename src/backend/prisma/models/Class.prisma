model Class {
  id   Int     @id @default(autoincrement())
  name String? @db.VarChar

  assignments       Assignment[]
  users             ClassUser[]
  groups            Group[]
  waitingroom_users User[]
}
