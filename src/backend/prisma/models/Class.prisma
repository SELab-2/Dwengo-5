model Class {
  id   Int     @id @default(autoincrement())
  name String? @db.VarChar

  assignments       Assignment[]
  class_users       ClassUser[]
  groups            Group[]
  waitingroom_users WaitingroomUser[]
}
